import * as target from './SolverService';
import { expect, describe, it } from 'vitest';

describe('SolverService', () => {
    it('is defined', () => {
        expect(target).toBeDefined();
    });
});
